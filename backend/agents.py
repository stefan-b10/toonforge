from crewai import Agent

class ComicsAgents:
    def __init__(self):
        self.llm = 

    def writer(self):
        return Agent (
            role="",
            goal="",
            backstory="",
            llm=self.llm,
            tools=[],
            verbose=True,
        )
    
    def screen_writer(self):
        return Agent (
            role="",
            goal="",
            backstory="",
            llm=self.llm,
            tools=[],
            verbose=True,
        )
    
    def artist(self):
        return Agent (
            role="",
            goal="",
            backstory="",
            llm=self.llm,
            tools=[],
            verbose=True,
        )