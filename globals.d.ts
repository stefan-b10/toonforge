declare type Story = {
  title: string;
  story: string;
  style: string;
  colors: [string];
};

declare type Scene = {
  image: string;
  text: string;
};

declare type FinalScene = {
  imageURL: string;
  text: string;
};
